import { Box } from '@mui/material';
import { styled } from '@mui/material';

export const Centered = styled(Box)`
  position: fixed;
  top: 50%;
  left: 50%;
  margin-top: -50px;
  margin-left: -100px;
`;
